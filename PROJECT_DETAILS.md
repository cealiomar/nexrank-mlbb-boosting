# NEXRANK — مشروع MLBB Boosting

موقع ثنائي اللغة (العربية / English) لخدمة رفع رانك **Mobile Legends: Bang Bang** للاعبين في مصر. يتيح للعميل حساب سعر واضح بالجنيه المصري، اختيار طريقة اللعب، ثم إرسال طلب للفريق لمراجعته قبل الدفع.

## الروابط

- الموقع: https://cealiomar.github.io/nexrank-mlbb-boosting/
- المستودع: https://github.com/cealiomar/nexrank-mlbb-boosting
- لوحة الطلبات: https://cealiomar.github.io/nexrank-mlbb-boosting/admin

> تنبيه: المستودع ولوحة الطلبات متاحان للعامة حالياً بناءً على إعداد GitHub Pages.

## فكرة المنتج

بدل نموذج طلب طويل أو تسعير غير واضح، رحلة العميل تبدأ مباشرة بآلة حاسبة بسيطة:

1. يحدد الرانك الحالي بدقة (Rank + Division + Stars).
2. يحدد الرانك أو عدد النجوم الذي يريد الوصول إليه.
3. يرى فاتورة فورية ومفصلة لكل نجمة باللغة التي اختارها.
4. يختار طريقة اللعب والإضافات المناسبة.
5. يرسل بيانات التواصل؛ يراجع الفريق الطلب ثم يتواصل معه قبل الدفع.

الخدمة ليست مرتبطة رسمياً بـ Mobile Legends: Bang Bang.

## الخدمات الموجودة

### 1. Rank Boost

رفع الرانك من نقطة حالية إلى هدف محدد. يدعم الرانكات التالية:

| الرانك | الدقة المتاحة | سعر النجمة (ج.م) |
| --- | --- | ---: |
| Warrior | III إلى I، 0–4 نجوم | 10 |
| Elite | IV إلى I، 0–4 نجوم | 12 |
| Master | IV إلى I، 0–4 نجوم | 15 |
| Grandmaster | V إلى I، 0–4 نجوم | 20 |
| Epic | V إلى I، 0–4 نجوم | 35 |
| Legend | V إلى I، 0–4 نجوم | 45 |
| Mythic | 0–24 نجمة | 65 |
| Mythical Honor | 25–49 نجمة | 80 |
| Mythical Glory | 50–99 نجمة | 100 |
| Mythical Immortal | 100–999 نجمة | 125 |

### 2. Self Play

العميل يلعب بنفسه مع الفريق بدلاً من أن ينفذ الفريق كامل الخدمة. يضاف **70%** على قيمة Rank Boost أو Hero Global.

### 3. Hero محدد

يمكن للعميل طلب أن يتم رفع الرانك بهيرو معين. يكتب اسم الهيرو بنفسه، وتضاف **15%** على الإجمالي.

### 4. Hero Global

خدمة منفصلة للصعود في ترتيب Hero Global. يحدد العميل اسم الهيرو، الـ Hero Power الحالي، والهدف (Top 500 / 100 / 50 / 10). يظهر تقدير أولي، ثم يراجعه الفريق قبل التأكيد النهائي.

## طريقة الحساب والفاتورة

- الفاتورة تحسب عدد النجوم بين البداية والهدف عبر كل رانك، وليس بسعر ثابت للرحلة كاملة.
- كل جزء يظهر منفصلاً: مثال `Epic: 8 نجوم × 35 ج.م`.
- الحد الأدنى للطلب: **150 ج.م** لخدمة Pilot و**250 ج.م** لخدمة Self Play.
- الإضافات تظهر كسطور مستقلة قبل الإجمالي النهائي.
- السعر المعروض هو **تقدير واضح**؛ يتم تأكيد التوفر وموعد التنفيذ مع العميل قبل استلام الدفع.

## Promo Codes التجريبية

| الكود | الخصم |
| --- | --- |
| `STREAM10` | 10% |
| `WELCOME100` | 100 ج.م |
| `GLOBAL15` | 15% |

يمكن تعديل الأكواد من دالة `applyPromo` داخل `app.js`.

## طرق الدفع الظاهرة للعميل

- InstaPay
- Vodafone Cash
- Binance
- PayPal

يختار العميل الطريقة المفضلة في نموذج الطلب. لا توجد بوابة دفع تلقائية حالياً؛ الدفع يتم بعد مراجعة الفريق للطلب.

## الطلبات والإشعارات

### بيانات التواصل للفريق

- WhatsApp: `+201030303397`
- Email: `cealiomar@gmail.com`

### ما الذي يحدث بعد «تأكيد الطلب»؟

1. يظهر نموذج لجمع الاسم، WhatsApp، الإيميل، السيرفر، وطريقة الدفع.
2. يُنشأ رقم طلب عشوائي.
3. يُحفظ الطلب مؤقتاً داخل متصفح العميل كنسخة احتياطية.
4. إذا تم ربط Google Apps Script، يُرسل الطلب إلى Google Sheet ويصل إيميل إلى `cealiomar@gmail.com`.
5. يتواصل الفريق مع العميل لتأكيد التوفر والسعر ووسيلة الدفع.

### تفعيل Google Sheets والإيميل

التكامل **غير مفعّل تلقائياً** لأن Google يتطلب تفويضاً من صاحب الحساب. الخطوات الكاملة موجودة في [GOOGLE_SETUP.md](GOOGLE_SETUP.md):

1. أنشئ Google Sheet باسم `NEXRANK Orders`.
2. الصق محتوى `google-apps-script/Code.gs` داخل Apps Script.
3. انشره كـ Web App بصلاحية الوصول المناسبة.
4. ضع رابط الـ Web App داخل `ordersWebhookUrl` في `config.js`.
5. ضع رابط الشيت داخل `ordersSheetUrl` في `config.js`.

بعد ذلك، كل طلب جديد يُضاف تلقائياً في Sheet ويصل إشعار على الإيميل.

## لوحة الإدارة

المسار: `/admin`

تتيح اللوحة عرض الطلبات المحفوظة في المتصفح، البحث فيها، وعدّاداً لإجمالي الطلبات وقيمتها. كما تعرض زر فتح Google Sheet بعد إضافة رابطه في `config.js`.

**مهم:** اللوحة الحالية واجهة بسيطة وليست محمية بتسجيل دخول. ولأن الموقع منشور للعامة، لا يجب استخدامها لتخزين بيانات حساسة أو بيانات دخول اللعبة. لإصدار Production حقيقي يفضّل إضافة مصادقة وإدارة طلبات على خادم آمن.

## التصميم وتجربة الاستخدام

آخر تحديث: 2026-09-04. تفاصيل التشخيص والقرارات في `docs/superpowers/specs/2026-09-04-mobile-ux-redesign-design.md`.

### اختيار الرانك — مقياس واحد

الرانك والـDivision مدمجان في **مقياس واحد من 30 خطوة** (`Warrior III` … `Legend I` … `Mythical Immortal`)، معروض كشريط أفقي من chips بـ`scroll-snap`. لا يوجد `<select>` للـDivision ولا `<input type="range">` للنجوم ولا بانل «بالتحديد» مخفي — كلها أُزيلت.

- **الطرفان**: زرّان (رانكك الحالي / هدفك) يحددان أيّ طرف يحرره الشريط. الشريط والنجوم يعملان دائماً على الطرف النشط.
- **النجوم**: خمس نجمات تُضغط مباشرة للرتب ذات الـDivision، و`− قيمة +` stepper لرتب Mythic وما فوقها.
- **الضبط الدقيق**: زرّا `−` / `+` بجانب الشريط يحركان خطوة واحدة.
- الحالة الداخلية بقيت `{rank, division, stars}` كما هي، ورقم الخطوة يُشتق منها — لذلك `rankInvoice()` و`bandsBetween()` و`absolutePosition()` لم تُمس ولم تتغير الأسعار.

### وضوح السعر

- كل خيار يعرض أثره على السعر بجانبه مباشرة (`السعر الأساسي` / `+70%` / `+15%` / `+25%`).
- عدّاد `تحتاج N نجمة` يتحدث مع كل تغيير في الطرفين.
- **على الموبايل**: شريط لاصق سفلي فيه الإجمالي وعدد النجوم وزر التأكيد؛ الضغط عليه يفتح bottom sheet بالفاتورة التفصيلية والبرومو وطرق الدفع. الإجمالي يومض عند تغيره.
- **على الديسكتوب** (≥1024px): الفاتورة عمود جانبي ثابت والشريط السفلي يختفي.

### الشكل والحركة

- كروت بحافتين (**double-bezel**): غلاف خارجي شفاف بحد شعري، وقلب داخلي بخلفية خاصة وإضاءة علوية `inset` — عشان العنصر يبان مركّب مش مسطّح.
- خلفية mesh من تدرّجات radial متعددة + طبقة grain ثابتة.
- الحركة كلها بـ`cubic-bezier(.32,.72,0,1)` — مفيش `linear` ولا `ease-in-out`.
- **دخول العناصر**: `IntersectionObserver` بيرفع كل بلوك من `translateY(28px) + blur(7px)` لوضعه الطبيعي على 900ms، مع تتابع 70ms بين عناصر المجموعة. مفيش عنصر بيظهر فجأة.
- **العدّاد**: الإجمالي بيعدّ من قيمته القديمة للجديدة على 520ms، ويقفز فورًا لو `prefers-reduced-motion` أو الصفحة مش بترسم.
- زرار الـCTA أيقونته في دايرة منفصلة بتتحرك قطريًا عند الـhover، ومع شعاع ضوء بيعدّي على الزرار.
- الـchip المختار بيكبر `scale(1.09)` مع توهج، وكل عنصر بيتضغط بينزل `scale(.975)`.

### الأسس

- واجهة عربية RTL مع زر تبديل للإنجليزية، وزر واتساب مباشر في الهيدر.
- أرقام لاتينية في اللغتين (`ar-EG-u-nu-latn`) لتتسق مع ما تعرضه حقول الإدخال، و`tabular-nums` على كل الأسعار.
- عزل bidi (`U+2066…U+2069`) حول `N★` حتى لا تنتقل النجمة إلى بداية السطر في RTL.
- `styles.css` نظام واحد mobile-first: مقياس مسافات ثابت، وحد أدنى **44px** لكل عنصر قابل للضغط. لا توجد طبقات media queries تتجاوز بعضها.
- مقياس الخط: النص الأساسي **14px**، وأصغر حجم عام **13px**. الاستثناء الوحيد هو الـeyebrow labels (uppercase + letter-spacing) عند **11px**، وسطران ثانويان عند **10px** (وحدة النجوم تحت العدّاد، وسطر الوصف في الشريط السفلي) — كلاهما مجاور لرقم كبير يحمل المعنى.
- الخطوط: `Alexandria` للنص العربي، و`Bricolage Grotesque` للعناوين والأرقام وأسماء الرتب مع `tabular-nums`.
- ألوان داكنة بطابع ألعاب تنافسي مع Lime أخضر كـ CTA واضح، وظلال مائلة للون الخلفية.
- خلفية WebGL خفيفة على الديسكتوب فقط، مع تدرّج ثابت على الموبايل، وكل الحركة محكومة بـ`prefers-reduced-motion`.

### تم التحقق منه

عرض 320 / 375 / 414 / 1280px: صفر تجاوز أفقي، صفر هدف لمس تحت 44px، صفر قصّ نص غير مقصود، صفر رسوب في تباين WCAG AA، وترتيب عناوين سليم يبدأ بـ`<h1>`.

الأسعار متطابقة مع ما قبل التعديل — تم التحقق يدويًا من: `Epic V→Mythic = 2,000`، و`Legend V+4★→Mythic = 945`، و`Warrior III→Immortal 109★ = 12,940`، ومضاعِفات Self Play و Express.

## ملفات المشروع المهمة

| الملف | وظيفته |
| --- | --- |
| `index.html` | صفحة العميل والآلة الحاسبة |
| `styles.css` | التصميم، الاستجابة للموبايل، والحركات |
| `app.js` | الرتب، التسعير، الفاتورة، الطلبات، واللغات |
| `admin.html` / `admin.js` | لوحة عرض الطلبات |
| `config.js` | رابط Google Apps Script وGoogle Sheet |
| `google-apps-script/Code.gs` | استقبال الطلب وإرساله إلى Google Sheet والإيميل |
| `GOOGLE_SETUP.md` | خطوات تفعيل Google عملياً |
| `build-sites.mjs` | بناء نسخة الاستضافة الأخرى |

## النشر والتحديث

الموقع منشور حالياً من فرع `main` عبر GitHub Pages. بعد أي تعديل:

1. احفظ التعديلات في المستودع.
2. ارفعها إلى فرع `main`.
3. GitHub Pages سيعيد النشر تلقائياً على الرابط نفسه خلال دقائق.

## تطويرات مقترحة للنسخة التالية

- إضافة تسجيل دخول آمن للمدير وإخفاء لوحة الإدارة عن الزوار.
- ربط WhatsApp Business API لإرسال تنبيه فوري للمدير.
- تفعيل بوابة دفع حقيقية أو صفحة تعليمات دفع حسب الوسيلة المختارة.
- صفحة تتبع طلب برقم الطلب + رقم الهاتف.
- لوحة تحكم لتعديل أسعار النجوم والأكواد من دون تعديل الكود.
- رفع ملف إثبات Hero Power إلى تخزين آمن بدلاً من إدخاله يدوياً فقط.
- تحليلات تحويل: من فتح الحاسبة إلى إرسال الطلب.
